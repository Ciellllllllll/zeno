[package]
name = "zeno_abi"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
zeno_core = { path = "../zeno_core" }
